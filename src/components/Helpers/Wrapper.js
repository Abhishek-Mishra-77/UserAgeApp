
const Wrapper = (props) => {
     return (props.children)
}

export default Wrapper;
// This is just Empty Component which work is to wrap the element like we did with <div> tag 
// when we are returning JSX code to wrap into the single element we can use this 
// Wrapper component like a <div> tag